machine:
  python:
    version: 2.7.11
  environment:
    DJANGO_SETTINGS_MODULE: library/library.settings
dependencies:
  pre:
    - pip install --upgrade pip
    - pip install --upgrade virtualenv
    - pip install coveralls
test:
  override:
    - coverage run library/manage.py test
    - COVERALLS_REPO_TOKEN=MJrEbmCkRiyN21w4uvw0fWnW7IKVNjaj9 coveralls
